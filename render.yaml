services:
  - type: web
    name: movie-genre-classification-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python train.py"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
